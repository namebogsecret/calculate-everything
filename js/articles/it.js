window.CE_ARTICLES = window.CE_ARTICLES || {};
Object.assign(window.CE_ARTICLES, {
  'number-base': `
<h2>Что это и что показывает</h2>
<p>Системы счисления — это разные способы записать одно и то же число. Десятичная (основание 10) привычна людям, двоичная (основание 2) — родной язык процессора, шестнадцатеричная (основание 16) компактно представляет байты, а восьмеричная (основание 8) встречается в правах доступа Unix. Калькулятор переводит число из любой из этих систем во все остальные, не меняя его величины — меняется только запись.</p>

<h2>Формула / как устроено</h2>
<p>Число в системе с основанием <code>b</code> — это сумма цифр, умноженных на степени основания. Для шестнадцатеричного <code>FF</code>:</p>
<p><span class="eq">FF = F·16¹ + F·16⁰ = 15·16 + 15 = 255</span></p>
<p>Перевод выполняется в два шага: сначала число приводится к десятичному значению, затем это значение раскладывается по степеням целевого основания. В коде это <code>parseInt(строка, основание)</code> и <code>число.toString(основание)</code>.</p>
<table>
<tr><th>Система</th><th>Основание</th><th>Цифры</th></tr>
<tr><td>Двоичная</td><td>2</td><td>0, 1</td></tr>
<tr><td>Восьмеричная</td><td>8</td><td>0–7</td></tr>
<tr><td>Десятичная</td><td>10</td><td>0–9</td></tr>
<tr><td>Шестнадцатеричная</td><td>16</td><td>0–9, A–F</td></tr>
</table>

<h2>Как посчитать вручную</h2>
<ol>
<li>Чтобы перевести в десятичную: умножьте каждую цифру на основание в степени её позиции (справа налево, начиная с 0) и сложите.</li>
<li>Чтобы из десятичной перевести в другую систему: делите число на основание, записывая остатки.</li>
<li>Соберите остатки снизу вверх — это и есть результат.</li>
<li>Проверьте обратным переводом в десятичную.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Переведём десятичное <code>255</code> в двоичную систему делением на 2:</p>
<p>255 / 2 = 127, остаток 1; 127 / 2 = 63, остаток 1; … все восемь остатков получаются единицами.</p>
<p>Результат: <code>11111111</code> (восемь единиц). Проверка: 128+64+32+16+8+4+2+1 = 255. А в шестнадцатеричной это <code>FF</code>.</p>
</div>

<h2>Где применяется</h2>
<p>Шестнадцатеричная запись используется для цветов в CSS, MAC-адресов, дампов памяти и байтовых данных. Двоичная нужна для понимания битовых масок, флагов и сетевых масок. Восьмеричная — это привычные права доступа Unix вроде <code>chmod 755</code>. Перевод систем счисления — базовый навык любого программиста и системного администратора.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Путают цифры разных систем: буква F допустима в HEX, но не в десятичной или двоичной.</li>
<li>Забывают, что позиции считаются с нуля справа: крайняя правая цифра умножается на основание в степени 0.</li>
<li>Принимают восьмеричное <code>010</code> за десятичное «десять» — на самом деле это 8.</li>
<li>Игнорируют регистр в HEX: <code>ff</code> и <code>FF</code> — одно и то же число.</li>
</ul>
</div>
`,
  'cidr': `
<h2>Что это и что показывает</h2>
<p>CIDR (Classless Inter-Domain Routing) — это способ записать IP-адрес вместе с размером подсети через косую черту: <code>192.168.1.0/24</code>. Калькулятор по такой записи определяет адрес сети, маску, broadcast-адрес, диапазон пригодных хостов и их количество. Это базовый инструмент при проектировании сетей и настройке маршрутизаторов.</p>

<h2>Формула / как устроено</h2>
<p>Префикс после слэша показывает, сколько старших бит из 32 относятся к адресу сети. Остальные биты — это хосты.</p>
<p><span class="eq">Всего адресов = 2^(32 − префикс)</span></p>
<p><span class="eq">Доступных хостов = всего адресов − 2</span></p>
<p>Адрес сети получается побитовым AND адреса и маски; broadcast — побитовым OR сети и wildcard-маски (инверсии маски).</p>
<table>
<tr><th>Префикс</th><th>Маска</th><th>Хостов</th></tr>
<tr><td>/24</td><td>255.255.255.0</td><td>254</td></tr>
<tr><td>/16</td><td>255.255.0.0</td><td>65 534</td></tr>
<tr><td>/8</td><td>255.0.0.0</td><td>16 777 214</td></tr>
</table>

<h2>Как посчитать вручную</h2>
<ol>
<li>Вычтите префикс из 32 — получите число бит, отведённых под хосты.</li>
<li>Возведите 2 в эту степень — это общее число адресов в подсети.</li>
<li>Вычтите 2 (адрес сети и broadcast) — получите число пригодных хостов.</li>
<li>Адрес сети: обнулите хостовые биты исходного адреса. Broadcast: установите их в единицы.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Подсеть <code>192.168.1.0/24</code>.</p>
<p>Бит под хосты: 32 − 24 = 8. Всего адресов: 2⁸ = 256.</p>
<p>Доступных хостов: 256 − 2 = <b>254</b>.</p>
<p>Адрес сети: <code>192.168.1.0</code>, broadcast: <code>192.168.1.255</code>, диапазон хостов: <code>192.168.1.1 — 192.168.1.254</code>.</p>
</div>

<h2>Где применяется</h2>
<p>CIDR используют при проектировании корпоративных и домашних сетей, настройке маршрутизаторов и firewall, разбиении больших сетей на подсети (subnetting), составлении ACL и при работе с облачными VPC (AWS, GCP, Azure). Понимание масок необходимо любому сетевому инженеру и DevOps.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Назначают устройству адрес сети (<code>.0</code>) или broadcast (<code>.255</code>) — они зарезервированы.</li>
<li>Забывают, что меньший префикс — это бо́льшая сеть: /16 крупнее /24.</li>
<li>Путают /31 и /32: для них правило «минус 2» не работает (point-to-point и одиночный хост).</li>
<li>Вводят октет больше 255 — каждый октет IPv4 умещается в один байт (0–255).</li>
</ul>
</div>
`,
  'color-converter': `
<h2>Что это и что показывает</h2>
<p>Конвертер цвета переводит один и тот же цвет между тремя представлениями: HEX (как в CSS — <code>#3b7a8c</code>), RGB (смешение красного, зелёного и синего) и HSL (тон, насыщенность, светлота). Цвет один и тот же — меняется лишь форма записи, удобная для разных задач: HEX для вёрстки, RGB для прозрачности, HSL для подбора оттенков.</p>

<h2>Формула / как устроено</h2>
<p>HEX-цвет из шести цифр — это три канала RGB по два символа: RR, GG, BB, каждый в шестнадцатеричном виде (0–255):</p>
<p><span class="eq">R = parseInt(RR, 16),&nbsp; G = parseInt(GG, 16),&nbsp; B = parseInt(BB, 16)</span></p>
<p>Для HSL каналы нормируются к диапазону 0–1, после чего светлота — это среднее максимума и минимума:</p>
<p><span class="eq">L = (max + min) / 2</span></p>
<p>Насыщенность и тон вычисляются из разницы между максимальным и минимальным каналом.</p>

<h2>Как посчитать вручную</h2>
<ol>
<li>Разбейте HEX на три пары символов и переведите каждую из шестнадцатеричной системы в десятичную — получите R, G, B.</li>
<li>Разделите каждый канал на 255, чтобы привести к диапазону 0–1.</li>
<li>Найдите максимальный и минимальный из трёх нормированных каналов.</li>
<li>Светлота L = (max + min) / 2; далее по формулам — насыщенность и тон.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Цвет <code>#3b7a8c</code>.</p>
<p>3B = 59, 7A = 122, 8C = 140, то есть <code>rgb(59, 122, 140)</code>.</p>
<p>Нормировка: 0.231, 0.478, 0.549. Светлота L = (0.549 + 0.231) / 2 ≈ 0.39 → 39%.</p>
<p>Итог в HSL: примерно <code>hsl(193, 41%, 39%)</code> — приглушённый сине-бирюзовый.</p>
</div>

<h2>Где применяется</h2>
<p>Конвертер нужен веб-разработчикам и дизайнерам: HEX в CSS, RGB при работе с прозрачностью (rgba), HSL для генерации палитр и тем (осветлить/затемнить можно простым изменением L). Также применяется при переносе цветов между Figma, графическими редакторами и кодом.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Путают порядок каналов: в HEX это R, G, B, а не B, G, R.</li>
<li>Считают, что трёхзначный <code>#abc</code> и шестизначный — разные цвета; на деле <code>#abc</code> = <code>#aabbcc</code>.</li>
<li>Указывают тон HSL в долях вместо градусов (0–360).</li>
<li>Забывают знак <code>%</code> у насыщенности и светлоты в записи HSL — без него CSS не примет значение.</li>
</ul>
</div>
`,
  'base64': `
<h2>Что это и что показывает</h2>
<p>Base64 — это способ представить любые данные (текст, картинки, файлы) с помощью 64 печатных символов: A–Z, a–z, 0–9, плюс и слэш. Калькулятор кодирует строку в Base64 и декодирует обратно, корректно работая с кириллицей за счёт UTF-8. Это не шифрование, а кодирование: результат легко вернуть к исходному виду.</p>

<h2>Формула / как устроено</h2>
<p>Base64 берёт данные по 3 байта (24 бита) и нарезает их на 4 группы по 6 бит. Каждая 6-битная группа (значение 0–63) заменяется символом из алфавита Base64.</p>
<p><span class="eq">3 байта (24 бита) → 4 символа Base64</span></p>
<p>Для безопасной работы с не-латиницей строка сначала кодируется в UTF-8: <code>btoa(unescape(encodeURIComponent(s)))</code>. Декодирование выполняет обратную цепочку.</p>

<h2>Как посчитать вручную</h2>
<ol>
<li>Переведите каждый символ строки в его байты (для ASCII — код символа).</li>
<li>Сгруппируйте биты по 3 байта и перепишите их как 4 группы по 6 бит.</li>
<li>Каждую 6-битную группу замените символом из таблицы Base64.</li>
<li>Если байтов не кратно 3 — дополните результат знаками <code>=</code> до кратности 4 символам.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Закодируем слово <code>Hi</code> (2 байта: 72 и 105).</p>
<p>Биты: 01001000 01101001 → группы по 6: 010010 000110 1001(00).</p>
<p>Это значения 18, 6, 36 → символы <code>S</code>, <code>G</code>, <code>k</code>. Недостаёт байта, поэтому хвост дополняется одним <code>=</code>.</p>
<p>Итог: <code>SGk=</code>.</p>
</div>

<h2>Где применяется</h2>
<p>Base64 используют для вложений в email (MIME), встраивания картинок в HTML и CSS через data-URI, передачи бинарных данных в JSON и XML, кодирования токенов (например, JWT состоит из Base64URL-частей) и хранения ключей. Это стандартный «клей» между двоичным миром и текстовыми протоколами.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Принимают Base64 за шифрование — данные раскодирует кто угодно, секреты так не защищают.</li>
<li>Кодируют кириллицу наивным <code>btoa</code> без UTF-8 — получают ошибку или «кракозябры».</li>
<li>Теряют padding-символы <code>=</code> при копировании — строка перестаёт декодироваться.</li>
<li>Путают обычный Base64 и Base64URL (где <code>+</code> и <code>/</code> заменены на <code>-</code> и <code>_</code> для безопасной вставки в URL).</li>
</ul>
</div>
`,
  'timestamp': `
<h2>Что это и что показывает</h2>
<p>Unix-timestamp — это число секунд, прошедших с полуночи 1 января 1970 года по UTC. Такой формат удобен компьютерам: одно целое число однозначно задаёт момент времени без привязки к часовому поясу. Калькулятор переводит timestamp в человекочитаемую дату и обратно — дату в timestamp.</p>

<h2>Формула / как устроено</h2>
<p>Timestamp измеряется в секундах, а JavaScript-объект <code>Date</code> — в миллисекундах, поэтому при переводе нужен множитель 1000:</p>
<p><span class="eq">дата = new Date(секунды · 1000)</span></p>
<p><span class="eq">timestamp = Date.parse(строка) / 1000</span></p>
<p>Точкой отсчёта (эпохой) служит 00:00:00 UTC 1 января 1970 года. Високосные секунды в Unix-времени не учитываются.</p>

<h2>Как посчитать вручную</h2>
<ol>
<li>Чтобы из timestamp получить дату: умножьте секунды на 1000 и подставьте в конструктор даты.</li>
<li>Грубая прикидка года: разделите timestamp на 31 557 600 (секунд в году) и прибавьте 1970.</li>
<li>Чтобы из даты получить timestamp: возьмите разницу в секундах между датой и началом эпохи.</li>
<li>Учитывайте часовой пояс: timestamp всегда в UTC, локальное время может отличаться.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Timestamp <code>1700000000</code>.</p>
<p>Прикидка года: 1 700 000 000 / 31 557 600 ≈ 53.9 года после 1970 → примерно конец 2023 года.</p>
<p>Точное значение: <code>2023-11-14T22:13:20Z</code> (UTC).</p>
<p>В миллисекундах это <code>1700000000000</code> — именно так его ждёт JavaScript.</p>
</div>

<h2>Где применяется</h2>
<p>Timestamp используют для хранения времени в базах данных, в логах серверов, при работе с API (поля created_at, expires_in), для вычисления разницы между событиями и в системах кэширования. Это универсальный формат, не зависящий от локали и часового пояса, поэтому его удобно сравнивать и сортировать.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Путают секунды и миллисекунды: 10-значное число — секунды, 13-значное — миллисекунды. Ошибка в 1000 раз сдвигает дату на тысячелетия.</li>
<li>Забывают про часовой пояс и считают локальное время равным UTC.</li>
<li>Игнорируют «проблему 2038 года» в старых 32-битных системах.</li>
<li>Передают дату без указания зоны и удивляются сдвигу — без зоны браузер трактует её как локальную.</li>
</ul>
</div>
`,
  'data-size': `
<h2>Что это и что показывает</h2>
<p>Конвертер размера данных переводит объём информации между битами, байтами и кратными единицами — килобайтами, мегабайтами, гигабайтами, терабайтами. Все эти единицы измеряют одну величину (количество информации), отличаясь лишь масштабом. Калькулятор использует двоичную меру, где каждая ступень в 1024 раза больше предыдущей.</p>

<h2>Формула / как устроено</h2>
<p>Все значения приводятся к байтам через множитель, а затем делятся на множитель целевой единицы. Бит — это одна восьмая байта:</p>
<p><span class="eq">байты = значение · множитель_источника</span></p>
<p><span class="eq">1 байт = 8 бит,&nbsp; 1 КиБ = 1024 байта</span></p>
<table>
<tr><th>Единица</th><th>В байтах</th></tr>
<tr><td>Бит</td><td>0.125</td></tr>
<tr><td>Байт</td><td>1</td></tr>
<tr><td>КиБ (КБ)</td><td>1 024</td></tr>
<tr><td>МиБ (МБ)</td><td>1 048 576</td></tr>
<tr><td>ГиБ (ГБ)</td><td>1 073 741 824</td></tr>
<tr><td>ТиБ (ТБ)</td><td>1 099 511 627 776</td></tr>
</table>

<h2>Как посчитать вручную</h2>
<ol>
<li>Определите множитель исходной единицы в байтах из таблицы.</li>
<li>Умножьте значение на множитель — получите объём в байтах.</li>
<li>Разделите байты на множитель нужной единицы.</li>
<li>Для перевода в биты умножьте байты на 8.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Сколько мегабайт в файле размером 1 ГиБ?</p>
<p>1 ГиБ в байтах: 1 · 1 073 741 824 = 1 073 741 824 Б.</p>
<p>В МиБ: 1 073 741 824 / 1 048 576 = <b>1024 МиБ</b>.</p>
<p>А в битах: 1 073 741 824 · 8 = 8 589 934 592 бит — отсюда и «гигабит» при оценке скорости передачи.</p>
</div>

<h2>Где применяется</h2>
<p>Конвертер нужен при оценке размера файлов и дискового пространства, расчёте трафика и скорости интернета (биты против байтов), планировании бэкапов, настройке квот и лимитов в облаке. Понимание разницы между двоичной и десятичной мерами объясняет, почему «1 ТБ» диск в системе показывает около 931 ГиБ.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Путают биты и байты: 100 Мбит/с — это всего около 12.5 МБ/с (делим на 8).</li>
<li>Смешивают двоичные (КиБ = 1024) и десятичные (КБ = 1000) единицы — отсюда «недостающие» гигабайты на диске.</li>
<li>Считают, что 1 МБ = 1000 КБ в системной мере; на самом деле 1024 КиБ.</li>
<li>Забывают умножить на 8 при переводе из байтов в биты и наоборот.</li>
</ul>
</div>
`,
  'password-entropy': `
<h2>Что это и что показывает</h2>
<p>Энтропия пароля — это мера его непредсказуемости в битах. Чем выше энтропия, тем больше вариантов нужно перебрать злоумышленнику и тем дольше идёт взлом методом грубой силы (brute force). Калькулятор оценивает энтропию по длине пароля и размеру использованного алфавита, а также прикидывает время полного перебора. Расчёт идёт только в вашем браузере.</p>

<h2>Формула / как устроено</h2>
<p>Энтропия равна длине пароля, умноженной на двоичный логарифм размера алфавита:</p>
<p><span class="eq">энтропия = длина × log₂(размер алфавита)</span></p>
<p>Размер алфавита складывается из используемых наборов символов:</p>
<table>
<tr><th>Набор</th><th>Прибавляет</th></tr>
<tr><td>Строчные a–z</td><td>26</td></tr>
<tr><td>Прописные A–Z</td><td>26</td></tr>
<tr><td>Цифры 0–9</td><td>10</td></tr>
<tr><td>Символы и прочее</td><td>33</td></tr>
</table>
<p>Полное число вариантов перебора равно размеру алфавита в степени длины пароля.</p>

<h2>Как посчитать вручную</h2>
<ol>
<li>Определите, какие наборы символов есть в пароле, и сложите их вклады — это размер алфавита.</li>
<li>Возьмите двоичный логарифм размера алфавита (например, log₂(95) ≈ 6.57).</li>
<li>Умножьте на длину пароля — получите энтропию в битах.</li>
<li>Сравните: менее 40 бит — слабый, 40–60 — средний, 60–80 — сильный, выше 80 — очень сильный.</li>
</ol>

<div class="example">
<h4>Пример</h4>
<p>Пароль <code>P@ssw0rd!</code> длиной 9 символов. В нём есть строчные, прописные, цифры и символы → алфавит 26+26+10+33 = 95.</p>
<p>log₂(95) ≈ 6.57. Энтропия: 9 · 6.57 ≈ <b>59 бит</b> — лишь «средний».</p>
<p>Парадокс: «случайно выглядящий» пароль слаб, потому что короткий и построен по известному шаблону. Длинная фраза из 4–5 слов даёт энтропию выше при той же запоминаемости.</p>
</div>

<h2>Где применяется</h2>
<p>Оценка энтропии используется в политиках безопасности, при выборе и настройке менеджеров паролей, проектировании требований к регистрации, аудите учётных записей. Она помогает понять, что важнее увеличивать длину, а не лепить «сложные» короткие пароли с заменами букв на символы.</p>

<div class="mistakes">
<h4>Осторожно</h4>
<ul>
<li>Считают, что замены вроде <code>a → @</code> сильно повышают стойкость — атакующие давно учитывают такие шаблоны.</li>
<li>Игнорируют словарные атаки: <code>Password123</code> ломается мгновенно, хотя формула даёт приличную энтропию.</li>
<li>Переоценивают короткие пароли: длина важнее набора символов.</li>
<li>Повторно используют один пароль на многих сайтах — высокая энтропия не спасёт при утечке базы.</li>
</ul>
</div>
`,
});
